# Evals

Blocking suite objectives:

- Mainline flow regression
- Search run status and page snapshot integrity
- Error code normalization for parameter anomalies
- Export masking and audit writes

Runner entrypoint: `python -m cvm_eval_runner.cli --suite blocking`
